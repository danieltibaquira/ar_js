/**
 * Entry point. The runtime wrapper (sketch registry, parameter panel, renderer
 * targets) is intentionally empty in this iteration — the project is in the
 * RED phase of its first TDD cycle and no feature code has been written yet.
 */

const root = document.getElementById('app');
if (root) {
  root.textContent = 'Islamic Geometry Explorer — TDD scaffold (red phase).';
}
